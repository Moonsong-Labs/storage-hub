import fs from "node:fs";
import path from "node:path";

async function main() {
  // launch SH Node

  // build fetch command

  // run fetch command

  // parse metadata

  // write metadata to file
}


main();
