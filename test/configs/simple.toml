[relaychain]
default_image = "docker.io/parity/polkadot:latest"
default_args = ["-lparachain=debug"]
chain = "rococo-local"

[[relaychain.nodes]]
name = "alice"
validator = true

[[relaychain.nodes]]
name = "bob"
validator = true

[[parachains]]
id = 2000
cumulus_based = true

[[parachains.collator]]
name = "storage-hub-collator-01"
image = "{{SH_IMAGE}}"
args = ["-lparachain=debug", "--unsafe-rpc-external", "--rpc-cors=all"]
