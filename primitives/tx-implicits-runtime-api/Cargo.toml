[package]
edition = "2021"
license = "GPL-3.0-only"
name = "shp-tx-implicits-runtime-api"
publish = false
version = "0.5.1"

[lib]
crate-type = ["rlib"]
name = "shp_tx_implicits_runtime_api"
path = "src/lib.rs"

[dependencies]
codec = { workspace = true }
scale-info = { workspace = true }
sp-api = { workspace = true }
sp-core = { workspace = true }
sp-runtime = { workspace = true }

[features]
default = ["std"]
std = [
    "codec/std",
    "scale-info/std",
    "sp-api/std",
    "sp-core/std",
    "sp-runtime/std",
]
