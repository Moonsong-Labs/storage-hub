{
  "name": "storagehub-e2e-dappwright",
  "version": "1.0.0",
  "description": "StorageHub SDK E2E tests using dAppwright for MetaMask automation",
  "type": "module",
  "scripts": {
    "test:metamask": "playwright test --project metamask",
    "test:msp": "playwright test --project msp",
    "serve:web": "bash -lc 'PIDS=\"$(lsof -ti tcp:3000 || true)\"; if [ -n \"$PIDS\" ]; then kill -9 $PIDS || true; fi; python3 -m http.server 3000 --directory ..'"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@tenkeylabs/dappwright": "^2.12.0",
    "playwright": "^1.56.1"
  },
  "engines": {
    "node": ">=22"
  }
}