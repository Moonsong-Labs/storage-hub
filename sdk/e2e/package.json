{
	"name": "storagehub-e2e-dappwright",
	"version": "1.0.0",
	"description": "StorageHub SDK E2E tests using dAppwright for MetaMask automation",
	"type": "module",
	"scripts": {
		"test:metamask": "playwright test --project metamask",
		"test:msp": "playwright test --project msp",
		"serve:web": "bash -lc 'PID=$(lsof -ti tcp:3000 || true); if [ -n \"$PID\" ]; then kill -9 \"$PID\"; fi; python3 -m http.server 3000 --directory ..'",
	},
	"devDependencies": {
		"@playwright/test": "^1.54.2",
		"@tenkeylabs/dappwright": "^2.11.2",
		"playwright": "^1.54.2"
	},
	"engines": {
		"node": ">=22"
	}
}